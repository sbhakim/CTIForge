You are a cyber threat intelligence analyst. You already extracted some triples from this text, but may have missed important relationships. Review the text again and extract ADDITIONAL triples that are NOT already in the existing list.

## Already Extracted
{% for t in existing_triples %}
- ({{ t.subject }}, {{ t.relation.value }}, {{ t.object }})
{% endfor %}

## What to Look For
Focus on relationships you may have missed:
- Alias/rebrand chains: "X also known as Y", "X evolved from Y" → (X, variant_of, Y)
- Delivery chains: intermediate steps in attack flow (e.g., "email delivers exploit", "exploit drops malware")
- Geographic connections: actors/victims/infrastructure located in countries/regions
- Tool usage: actors using specific tools, techniques, or infrastructure
- Data targets: what data is stolen, encrypted, or exfiltrated
- Attribution: which actor is behind which campaign or malware

## Output Format
Return ONLY new triples not already listed above, in the same JSON format:
```json
{
  "triples": [
    {
      "subject": "entity name",
      "subject_type": "ThreatActor|Malware|Tool|Campaign|Vulnerability|Technique|Organization|Location|IOC|Infrastructure|Software|Other",
      "relation": "uses|targets|exploits|delivers|communicates_with|drops|attributed_to|associated_with|variant_of|located_in|mitigated_by|related_to",
      "object": "entity name",
      "object_type": "ThreatActor|Malware|Tool|Campaign|Vulnerability|Technique|Organization|Location|IOC|Infrastructure|Software|Other",
      "evidence": "exact sentence from text"
    }
  ]
}
```

If no additional triples can be found, return {"triples": []}.

## Text
{{ text }}